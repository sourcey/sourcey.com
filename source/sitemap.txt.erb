<% root_url = data.site.url %>
<% sitemap.resources.each do |page| %>
<%= "#{root_url}#{page.url}\n" if (page.url !~ /\.(|css|js|eot|svg|woff|ttf|png|jpg)$/ && page.url !~ /bower_components/) %>
<% end %>